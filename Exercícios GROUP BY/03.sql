SELECT users.name AS writer, COUNT(testimonials."writerId") AS testimonialCount FROM users JOIN testimonials ON testimonials."writerId"=users.id WHERE testimonials."writerId" = 435 GROUP BY users.name;